// function loggerFunction(list, amount) {
//   console.log(`You have ${amount} tasks left`);
//   for (let i = 0; i < amount; i++) {
//     console.log(
//       `To Do List: ${list[i].title} | due: ${list[i].dueDate} | description: ${list[i].description} | priority: ${list[i].priority}`
//     );
//   }
// }
